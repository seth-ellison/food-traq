When multiple DAOs are used together frequently, we create a service which provides a united,
sensible way to access the data from a single object.

Never create a service that only uses a single DAO. In that case, the wrapper is not needed.